<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Booking Confirmation</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      padding: 20px;
      font-family: "Segoe UI", sans-serif;
      background: #f2f2f2;
    }
    .confirmation-card {
      max-width: 500px;
      margin: auto;
      background: white;
      border-radius: 16px;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
      padding: 24px;
      text-align: center;
    }
    .emoji {
      font-size: 3rem;
    }
  </style>
</head>
<body>
  <div class="confirmation-card">
    <div class="emoji">✅</div>
    <h2 class="text-success">Thank You, <%= name %>!</h2>
    <p class="lead">Your enquiry has been received.</p>

    <h5 class="mt-4">📋 Booking Summary</h5>
    <ul class="list-group text-start mb-3">
      <li class="list-group-item"><strong>Email:</strong> <%= email %></li>
      <li class="list-group-item"><strong>Mobile:</strong> <%= mobile %></li>
      <li class="list-group-item"><strong>Service Type:</strong> <%= serviceType %></li>
      <li class="list-group-item"><strong>Pickup:</strong> <%= from %> → <%= to %></li>
      <li class="list-group-item"><strong>Pickup Date & Time:</strong> <%= formattedDate %> <%= formattedTime %></li>
      <% if (dropDateTime) { %>
        <li class="list-group-item"><strong>Drop Date & Time:</strong> <%= dropDateTime %></li>
      <% } %>
    </ul>

    <p>📞 We'll contact you soon to confirm.</p>
    <a href="/" class="btn btn-primary mt-3">Back to Home</a>
  </div>
</body>
</html>
