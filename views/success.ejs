<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Booking Confirmation</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      margin: 0;
      background: #e6f9f0;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Segoe UI", sans-serif;
    }

    .popup {
      background: #fff;
      border-radius: 16px;
      max-width: 550px;
      width: 90%;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      animation: fadeIn 0.4s ease-out;
      overflow: hidden;
    }

    .popup-header {
      background-color: #22d64b;
      padding: 16px 24px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .popup-header img {
      height: 40px;
    }

    .popup-header .close-btn {
      font-size: 24px;
      color: #fff;
      cursor: pointer;
      transition: color 0.3s ease;
    }

    .popup-header .close-btn:hover {
      color: #000;
    }

    .popup-body {
      padding: 40px 24px;
      text-align: center;
    }

    .popup-body img {
      width: 250px;
    }

    .popup-body h3 {
      color: #28a745;
      font-weight: 700;
      margin-bottom: 15px;
      font-size: 24px;
    }

    .popup-body p {
      color: #555;
      font-size: 16px;
      margin-bottom: 10px;
    }

    .popup-body hr {
      margin: 20px 0;
    }

    .popup-body ul {
      padding: 0;
      list-style: none;
      margin-bottom: 20px;
    }

    .popup-body li {
      padding: 10px 15px;
      border: 1px solid #ddd;
      border-radius: 6px;
      margin-bottom: 8px;
      background: #f8f9fa;
      font-size: 15px;
      text-align: left;
    }

    .call-now-btn {
      background-color: #0f91fb;
      color: white;
      padding: 12px 25px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      font-size: 16px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .call-now-btn:hover {
      background-color: #e06d00;
      transform: scale(1.05);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 576px) {
      .popup-body {
        padding: 30px 15px;
      }

      .popup-header {
        padding: 12px 16px;
      }

      .popup-body img {
        width: 200px;
      }
    }
  </style>
</head>

<body>
  <div class="popup">
    <!-- Header -->
    <div class="popup-header">
      <img src="/assets/images/car/logo1.webp" alt="Garuda Travels Logo">
      <span class="close-btn" onclick="window.location.href='/'">&times;</span>
    </div>

    <!-- Body -->
    <div class="popup-body">
      <img src="/assets/images/car/Confimation.png" alt="Booking Confirmed" />
      <h3>Thank you, <%= name %>!</h3>
      <p>Your enquiry has been submitted successfully.</p>

      <hr>

      <p><strong>Booking Summary</strong></p>
      <ul>
        <li><strong>Email:</strong> <%= email %></li>
        <li><strong>Mobile:</strong> <%= mobile %></li>
        <li><strong>Service:</strong> <%= serviceType %></li>
        <li><strong>Pickup:</strong> <%= from %> → <%= to %></li>
        <li><strong>Date & Time:</strong> <%= formattedDate %> <%= formattedTime %></li>
        <% if (dropDateTime) { %>
          <li><strong>Drop Date & Time:</strong> <%= dropDateTime %></li>
        <% } %>
      </ul>

      <a href="tel:+9840789857" class="call-now-btn">Call Now</a>
    </div>
  </div>
</body>
</html>
